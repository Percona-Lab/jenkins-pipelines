- job:
    name: pxc-package-testing-pyinfra
    project-type: pipeline
    description: |
        PXC package testing (3-node cluster) using pyinfra instead of molecule/ansible.
        Do not edit this job through the web!
    concurrent: true
    properties:
        - build-discarder:
            artifact-days-to-keep: -1
            artifact-num-to-keep: 100
            days-to-keep: -1
            num-to-keep: 100
    pipeline-scm:
        scm:
            - git:
                url: https://github.com/Percona-Lab/jenkins-pipelines.git
                branches:
                    - 'master'
                wipe-workspace: true
        lightweight-checkout: true
        script-path: pxc/jenkins/pxc-package-testing-pyinfra.groovy
