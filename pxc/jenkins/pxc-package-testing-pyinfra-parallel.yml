- job:
    name: pxc-package-testing-pyinfra-parallel
    project-type: pipeline
    description: |
        Fan-out orchestrator for pxc-package-testing-pyinfra - runs the pyinfra
        PXC package tests on all supported OSes in parallel.
        Do not edit this job through the web!
    concurrent: true
    properties:
        - build-discarder:
            artifact-days-to-keep: -1
            artifact-num-to-keep: 100
            days-to-keep: -1
            num-to-keep: 100
    pipeline-scm:
        scm:
            - git:
                url: https://github.com/Percona-Lab/jenkins-pipelines.git
                branches:
                    - 'master'
                wipe-workspace: true
        lightweight-checkout: true
        script-path: pxc/jenkins/pxc-package-testing-pyinfra-parallel.groovy
